interface:
  display_name: "vLLM MM Precision"
  short_description: "Run multimodal three-mode regression and retest flows"
  default_prompt: "Use $vllm-multimodal-precision-testing to run the local multimodal regression stack across base64, local_path, and http, including L0, L0.5, MME, and MMBench checks, and prefer the standard retest entrypoint when capability plus precision need a unified artifact drop."
