interface:
  display_name: "vLLM Multimodal Evaluator"
  short_description: "Run multimodal capability checks across three media modes"
  default_prompt: "Use $vllm-multimodal-evaluator to generate deterministic image and video fixtures, start a local Qwen3.5 multimodal service if needed, and run the multimodal capability checklist across file_url, base64, and http with a PASS or FAIL report."
