interface:
  display_name: "vLLM Multimodal Evaluator"
  short_description: "Run multimodal capability checks across three transport modes"
  default_prompt: "Use $vllm-multimodal-evaluator to generate deterministic image and video fixtures, verify every capability item across local_path, base64, and http, include 1~10 multi-video understanding plus large-image smoke and function-calling checks, and keep Phase 1 ingestion, Phase 2 semantics, and clear failure attribution separated."
