interface:
  display_name: "vLLM Ascend API Server Profiler"
  short_description: "Profile and analyze API server hotspots on Ascend"
  default_prompt: "Use $vllm-ascend-api-server-profiler to profile stock vllm-ascend API server hotspots, break them into stage_spans and concurrency_windows, explain the internal logic of each hotspot, and give multiple optimization options before we change any code."
