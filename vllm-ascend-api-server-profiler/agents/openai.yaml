interface:
  display_name: "vLLM Ascend API Server Profiler"
  short_description: "Profile stage-based API server bottlenecks on Ascend"
  default_prompt: "Use $vllm-ascend-api-server-profiler to profile stock vllm-ascend API server hotspots, break them into stage_spans and concurrency_windows, and summarize the bottlenecks for later optimization."
